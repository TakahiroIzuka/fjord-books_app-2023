<h1><%= t 'views.common.title_index', name: User.model_name.human %></h1>

<div id="users">
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.email, user %></li>
    <% end %>
  </ul>
</div>

<%= paginate @users %>
